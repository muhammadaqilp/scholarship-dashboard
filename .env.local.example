# Copy this file to .env.local (already gitignored) and set your real code.
# This is the one place the product's access code is configured - nothing
# else in the codebase references it. After changing it, re-run
# "npm run dev" or "npm run build" to regenerate its hash.
SCHOLARSHIP_QUEST_ACCESS_CODE=SQ-2026-XXXX
